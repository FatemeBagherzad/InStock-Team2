import { Link } from "react-router-dom";

function WarehouseButton() {
  return (
    <Link to="">
      <button className="wButton">
        <p className="wButton__p">Warehouses</p>
      </button>
    </Link>
  );
}

export default WarehouseButton;
